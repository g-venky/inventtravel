

<%= form_for @topic do |f| %>
 
  <% if @topic.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@topic.errors.count, "error") %> prohibited
        this topic from being saved:
      </h2>
      <ul>
        <% @topic.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
 
  <p>
    <%= f.label :topic_for_discussion %><br>
    <%= f.text_field :discussion %>
  </p>
 
  <p>
    <%= f.label :details %><br>
    <%= f.text_area :details %>
  </p>
 
  <p>
    <%= f.submit :share %>
  </p>
 
<% end %>